# requirements.txt — کتابخانه‌های مورد نیاز پروژه TSP
# نصب: pip install -r requirements.txt

# --- لایه داده (فاز ۱) ---
requests>=2.31.0          # ارسال درخواست‌های HTTP به API

# --- رابط کاربری (فاز ۴) ---
PyQt6>=6.6.0              # رابط گرافیکی اصلی
matplotlib>=3.8.0         # رسم نمودار و نمایش گراف شهرها
networkx>=3.2.0           # ساختار داده گراف برای نمایش بصری

# --- الگوریتم‌های پرولاگ (فاز ۲) ---
pyswip>=0.2.10            # اتصال Python به SWI-Prolog

# --- ماشین لرنینگ (فاز ۳) ---
scikit-learn>=1.4.0       # درخت تصمیم و ذخیره مدل
numpy>=1.26.0             # محاسبات عددی (وابستگی scikit-learn)
joblib>=1.3.0             # ذخیره‌سازی فایل pkl مدل
